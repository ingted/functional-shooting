{application, shootingserver,
 [
  {description, ""},
  {vsn, "1"},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib
                 ]},
  {mod, { shootingserver_app, []}},
  {env, [
    {lager, [
      {handlers, [
        {lager_console_backend, debug},
          {lager_file_backend, [
            {"log/error.log", error, 1048760, "$D05", 5},
            {"log/console.log", debug, 1048760, "$D05", 5}
          ]}
      ]}
    ]}
  ]}
 ]}.
